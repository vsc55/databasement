apiVersion: v1
kind: Service
metadata:
  name: {{ include "databasement.fullname" . }}
  labels:
    {{- include "databasement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "databasement.selectorLabels" . | nindent 4 }}
