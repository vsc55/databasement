Databasement has been deployed!

{{- $fullName := include "databasement.fullname" . }}
{{- $namespace := .Release.Namespace }}

{{- if .Values.ingress.enabled }}

Access the application at:
  http{{ if .Values.ingress.tlsSecretName }}s{{ end }}://{{ .Values.ingress.host }}

{{- else }}

Access the application via port-forward:
  kubectl port-forward svc/{{ $fullName }} {{ .Values.port }}:{{ .Values.service.port }} -n {{ $namespace }}

Then open: http://localhost:{{ .Values.port }}

{{- end }}

Service endpoint:
  {{ $fullName }}.{{ $namespace }}.svc.cluster.local:{{ .Values.service.port }}

Check status:
  kubectl get pods -n {{ $namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
